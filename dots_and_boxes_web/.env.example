# Frontend configuration (Create React App)
# Ask the orchestrator/user to set these in dots_and_boxes_web .env

# Base URL for REST calls to the FastAPI backend (no trailing slash).
# Example (local): http://localhost:3001
# Example (preview): https://<host>:3001
REACT_APP_API_BASE_URL=

# Base URL for WebSocket connection to the FastAPI backend.
# Example (local): ws://localhost:3001
# Example (preview): wss://<host>:3001
REACT_APP_WS_BASE_URL=
